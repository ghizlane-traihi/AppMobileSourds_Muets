Trois parties:
** Frontend mobile react native (ghizlane)
**Backend API + Audio (imane)

**IA GESTES +DATASET (MINE):

__ Enregistrer mon travail au fur et a mesure sur Github:
   Initialiser Git dans mon projet:
       cd ~/IA_SOURD_MUET
       git init
   connecter a mon Github:
        git remote add origin https://github.com/TON_USERNAME/sign-language-ai.git
   Ajouter mes fichiers
        git add .
   Faire un premier commit
        git commit -m "Initial commit - IA langue des signes"
   Envoyer sur Github
        git branch -M main
        git push -u origin main
   Mettre a jour 
        git add .
        git commit -m "update: amélioration modèle / camera / bug fix"
        git push
__Installer Python

__Installer Visual Studio Code

__Créer un environnement virtuel: python -m venv venv
  source venv/bin/activate (linux)
  venv\Scripts\activate (windows)
  
__Installer les librairies IA  :  pip install opencv-python mediapipe torch numpy matplotlib pandas scikit-learn 
   mediapipe: reconnaitre position des mains avec des coordonnees(21 coordonnees xi yi zi)

__Test rapide caméra + main:
	import cv2
	import mediapipe as mp

	mp_hands = mp.solutions.hands.Hands()
	cap = cv2.VideoCapture(0)

	while True:
	    ret, frame = cap.read()
	    rgb = cv2.cvtColor(frame, cv2.COLOR_BGR2RGB)
	    result = mp_hands.process(rgb)

	    if result.multi_hand_landmarks:
		for hand in result.multi_hand_landmarks:
		    mp.solutions.drawing_utils.draw_landmarks(
		        frame, hand, mp.solutions.hands.HAND_CONNECTIONS)

	    cv2.imshow("Hand", frame)

	    if cv2.waitKey(1) & 0xFF == 27:
		break

	cap.release()
	cv2.destroyAllWindows()
	opencv-python mediapipe torch numpy matplotlib

__ verifier Installation PyTorch( pour faire un modele Long Short-Term Memory (LSTM) :est un type de réseau de neurones spécialisé pour les données dans le temps)
   import torch
   print(torch.__version__)

__création du dataset
   Dataset landmarks sur kaggle :https://www.kaggle.com/datasets/jaisuryaprabu/sign-language-landmarks?resource=download
   sans LSTM au debut(car pas de mouvement temporel)
	✔ Utiliser dataset alphabet 
	✔ Entraîner modèle classification
	✔ Tester en temps réel avec caméra


